pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout code from version control
                sh 'echo hi'
            }
        }
        stage('Deploy') {
            steps {
                // Deploy the application
                sh 'echo bue'
            }
        }
    }
}
